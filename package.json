{
  "name": "kaiserreich-run-picker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": { "test": "node --test", "start": "node scripts/serve.mjs" },
  "engines": { "node": ">=22" }
}
